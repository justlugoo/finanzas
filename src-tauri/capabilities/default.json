{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacidades por defecto para la ventana principal",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
